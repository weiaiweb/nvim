require("toggleterm").setup({
    -- Ctl + \ 打开终端
    -- open_mapping = [[<c-\>]],
    -- 打开新终端后自动进入插入模式
    start_in_insert = true,
    -- 打开终端的方式
    direction = 'float',  -- 'vertical' | 'horizontal' | 'tab' | 'float',
})
